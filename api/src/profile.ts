import { Controller, Get, Req, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { PrismaService } from './prisma.service'; import { JwtAuthGuard } from './auth';
@ApiTags('profile') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('v1/profile') export class ProfileController { constructor(private db:PrismaService){} @Get('me') me(@Req() req:any){return this.db.user.findUniqueOrThrow({where:{id:req.user.sub},select:{id:true,name:true,username:true,email:true,avatarUrl:true,preferredGenres:true,preferredCreationType:true,onboardingCompleted:true,subscriptionTier:true,_count:{select:{stories:true}}}})} }
